{
  "name": "uuid-bruteforce",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts",
    "compile": "bun build src/index.ts --compile --outfile build/uuid-bruteforce-benchmark.exe",
    "execute": "build/uuid-bruteforce-benchmark.exe"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "uuid": "^13.0.0"
  }
}
